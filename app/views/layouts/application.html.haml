!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title TestAppRails
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    %script{:src => "http://maps.google.com/maps/api/js?sensor=false&libraries=geometry&v=3.7"}
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    %link{:href => "https://fonts.googleapis.com/icon?family=Material+Icons", :rel => "stylesheet"}/
    = csrf_meta_tags
  %body

    .navbar.navbar-default
      .container
        .navbar-header
          = link_to "Catálogo de discos", root_path, class: "navbar-brand"
        %ul.nav.navbar-nav
          %li
            = link_to "Cadastrar", new_user_registration_path
            - if user_signed_in?
              %li
                = link_to "Sair", destroy_user_session_path, method: :delete
            - else 
              %li
                = link_to "Logar", new_user_session_path

        %ul.nav.navbar-nav.navbar-right
          - if user_signed_in?
            %li
              = link_to current_user.email, edit_user_registration_path
          - else
            %li
              %a Bem vindo, Visitante
    .container
      .row
        = yield
